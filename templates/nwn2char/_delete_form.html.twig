<form method="post" action="{{ path('nwn2chars_delete', {'id': nwn2chars.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ nwn2chars.id) }}">
    <button class="btn btn-sm btn-danger mt-2">Delete</button>
</form>
